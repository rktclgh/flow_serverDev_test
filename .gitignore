.env
.env.prod
.DS_Store

# OMC 작업 상태 (운영 산출물)
.omc/

# 과제 원문(회사 문서) — 공개 저장소에 올리지 않는다
과제_파일업로드_AI개발.md

# 내부 설계 문서 — 구현 지시서. 제출물이 아니다.
SPEC.md

# 제출용 문서 — 제출 시점에 직접 전달한다.
# 저장소에 두면 편집·수정 과정이 커밋 히스토리에 그대로 남는다.
PROMPT_LOG.md
CONSIDERATIONS.md

# jqwik 이 실패 케이스를 재현하려고 남기는 로컬 캐시
.jqwik-database

# 작업 지침 — 인프라 접속 정보와 민감정보가 들어간다. 절대 커밋하지 않는다.
CLAUDE.md
